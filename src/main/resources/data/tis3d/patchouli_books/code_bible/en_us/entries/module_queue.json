{
    "name": "Queue Module",
    "icon": "tis3d:module_queue",
    "category": "tis3d:modules",
    "pages": [
        {
            "title": "",
            "text": "The queue module is capable of storing up to sixteen (16) values. It can act as expanded memory for $(l)$(l:module_execution)execution modules$(), for example. $(br) $(br)While not full, the queue module reads values from all four of its ports and pushes read values to the end of the list of stored values. While not empty, the queue module",
            "type": "text"
        },
        {
            "title": "",
            "text": "writes the head value, i.e. the value that was first pushed to the internal list of values, to all four of its ports. In other words, the queue module is FIFO buffer. $(br) $(br)A value from the queue can always only be transferred to one port, i.e. values will never be duplicated; even when multiple reads would occur in one $(l)$(l:controller)controller$() cycle, only one will succeed. Unlike the $(l)$(l:module_stack)stack module$(), the queue module does not reset its write",
            "type": "text"
        },
        {
            "title": "",
            "text": "operations when a value is written to it. $(br)",
            "type": "text"
        }
    ]
}